\documentclass[12pt,a4paper,oneside,russian]{book}

\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}
\usepackage{amsmath}
%\usepackage[]{graphicx}
\usepackage{longtable}
\usepackage[unicode,colorlinks=true,bookmarks,bookmarksopen=true,bookmarksnumbered=true,bookmarksopenlevel=1,linkcolor=blue]{hyperref} 
%\usepackage[14pt]{extsizes}

%\newenvironment{code}
%\leftmargin{1em}}

\makeindex

\selectlanguage{russian}

\oddsidemargin -5mm
\textwidth 180mm
\textheight 260mm
\topmargin -20mm
\parindent 2em

\title{Техническое задание на программу для \\просмотра графических изображений}
\author{A.\,Zhidenkov}
\date{ \today }

\begin{document}

\maketitle

\tableofcontents

\chapter{Общие требования}

Программа \textit{Jemmo} предназначена для просмотра файлов графических изображений различных форматов
в среде Microsoft Windows. Представляет собой исполняемых файл Jemmo.exe и программы-библиотеки, необходимые
для её работы.

Основным критерием в оценке работы программы является её быстродействие, а именно, способность максимально
быстро отображать файлы большого размера.

Для работы с фалами формата JPEG используется библиотека jpeglib, статически компонуемая с приложением на
этапе его сборки. В качестве среды разработки и компилятора используется Microsoft Visual C++ 6. Также
необходимо наличие Windows SDK.

\chapter{Основные характеристики}

\section{Режимы работы}

Программа должна уметь работать в двух режимах -- режиме просмотра изображения и в режиме навигации по
изображениям.

\subsection{Режим просмотра изображения}

В режиме просмотра изображения показывается только одно изображение. При этом необходимо реализовать
набор простейших операций.

\begin{itemize}
\item[-] Масштабирование
\item[-] Поворот на 90 градусов по- и против часовой стрелки
\item[-] Перемещение по изображению в случае, если оно не помещается в окно просмотра,
как с помощью клавиш, так и с помощью мыши.
\end{itemize}

\section{Поведение программы при запуске, параметры коммандной строки}

Программа может запускаться с параметрами коммандной строки или без них. При этом, если параметр коммадной строки
не указывается программа должна вести себя так, как будто в качестве параметра была указана текущая директория.

Если в качстве параметра указано имя файла, необходимо открыть его в режиме просмотра. Если в качестве параметра
указана директория, то программа составляет список всех файлов поддерживаемых форматов в этой директории и отображает
первый из них в режиме просмотра изображения.

\section{Определение формата файла}

В первой версии программы предположение о формате изображения делается исключительно по расширению файла. Например, если
файл имеет расширение \textbf{.jpeg}, \textbf{.jpg} или \textbf{.jp}, то предполагается, что изображение в файле
хранится в формате JFIF. Если предположение оказывается ошибочным, то картинка просто не отображается. Алгоритм предполагает
регистронезависимость символов в расширении.
                                       
\end{document}
